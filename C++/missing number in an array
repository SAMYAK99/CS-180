class Solution{
  public:
    int MissingNumber(vector<int>& array, int n) {
    
        int tsum=(n*(n+1))/2,sum=0;
        for(int i=0;i<n-1;i++){
            sum=sum+array[i];
            }
            return tsum-sum;
    }
};
