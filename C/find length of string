#include<stdio.h>
#include<conio.h>
#include<string.h>
int main()
{
   char str[50];
   int count=0;

   printf("\n Enter Your String.");
   scanf("%[^\n]s",&str);
  // puts(str);
  for(i=0;str[i]!='\0';i++)
  {
  	count++;
  }
  printf("The lenght of string is = %d",count);
   return 0;
}
